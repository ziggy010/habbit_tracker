import 'package:flutter/material.dart';

class habbitTile extends StatelessWidget {
  const habbitTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(24),
    );
  }
}
